interface:
  display_name: "YouTube Caption Summary"
  short_description: "Fetch YouTube captions directly and summarize in your language."
  default_prompt: "Use $youtube-caption-summary to fetch captions directly from this YouTube link and return a grounded summary plus mentioned items in my language."
